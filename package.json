{
  "name": "madder-models",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm --workspace=apps/web run dev",
    "api": "python apps/api/main.py",
    "build": "npm --workspaces run build",
    "typecheck": "npm --workspaces run typecheck"
  }
}
