import { edgeTable, triTable } from "three-stdlib";

export const EDGE_TABLE: number[] = Array.from(edgeTable as unknown as ArrayLike<number>);
export const TRI_TABLE: number[] = Array.from(triTable as unknown as ArrayLike<number>);
